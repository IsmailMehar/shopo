Folder explanation
backend/ contains the Express server, database connection, and API route handlers.
database/ contains the SQL script used to create the database schema and insert sample product data.
frontend/ contains the React application, UI components, and styling files.